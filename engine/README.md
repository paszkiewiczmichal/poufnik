# anonymizer-engine

Offline document anonymization engine for Poufnik: parsing, OCR, detection,
anonymization, and export, tuned for Polish-language documents. Runs as a
local sidecar process talking only to the desktop app on `127.0.0.1` - no
outbound network calls.

See the repository root README for the full project overview.
